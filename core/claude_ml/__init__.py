"""
core/claude_ml — contexto ML para Claude (SRP + Facade + Strategy de dosagem).

  estado      — como o ML está agora
  stress     — termômetro do produto/nicho
  dosagem    — Strategy profundidade mínima/padrão/ampliada
  enriquecedor — Facade que monta o JSON para o prompt

API pública: core.claude_contexto_ml
"""
